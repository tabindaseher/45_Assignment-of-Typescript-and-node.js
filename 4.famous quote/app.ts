//store a quote in a variable

let quote: string = "A winner is a dreamer who never gives up";
//store a author name in variable
let author: string = "Nelson Mandela";
//print a quote with author name
console.log(`${author}: once said, "${quote}"`);